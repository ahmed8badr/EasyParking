<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyParking">
        <activity android:name=".TimeAndDate" />
        <activity android:name=".dBlock" />
        <activity android:name=".cBlock" />
        <activity android:name=".bBlock" />
        <activity android:name=".aBlock" />
        <activity android:name=".Your_Booked" />
        <activity android:name=".YourProfile" />
        <activity android:name=".Booking" />
        <activity android:name=".Menu_Activity" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>